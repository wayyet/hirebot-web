# 雇佣端 UX 文档 V6

**文档目的：**
基于《01-产品设计备忘录》《03-雇佣端详细需求规格说明书》与现有雇佣端原型，对雇佣端的页面级 UX 设计、角色化信息架构、关键流程和跳转关系进行重写与整理，形成一份更便于产品、交互、UI、研发、测试协同使用的版本。

**适用对象：**
产品、交互、UI、前后端研发、测试

**文档说明：**

1. 本文以需求规格为准，对原型中未完整覆盖的页面、角色差异和流程回退做结构化补齐。
2. 本版按最新讨论结果，移除"工作台"模块与全部反馈链路。
3. 本版将"团队数字员工"和"部门可用"统一为一个模块名：`部门数字员工`。
4. **v6 变更**：新增"部门长快捷复制"流程；将站内对话升级为主要对话渠道；IM 接入改为可选，支持飞书/钉钉/企微三平台并行配置；新增页面：快捷复制确认页、站内对话页、IM 配置页（多平台）。

---

## 1. 设计目标与原则

### 1.1 设计目标

雇佣端需要同时满足三类目标：

1. 让部门长可以基于模板完成部门版数字员工的完整雇佣流程，并持续查看团队内数字员工的状态变化。
2. **让部门长可以从已有部门员工快速复制出新的部门员工，跳过评估直接上岗。**
3. 让普通成员可以从部门已上岗的数字员工中快速复制出自己的个人分身，**立即在平台内通过站内对话开始使用**，并可按需接入 IM。

因此，系统必须同时支持"完整雇佣流程"、"快捷复制流程"和"极简分身复制流程"，但不能把三类流程混在一起。

### 1.2 开箱即用

1. 普通成员复制数字员工时，路径必须压缩到最少步骤。
2. 普通成员不进入模板池，不进入六步雇佣流程，不进入雇佣教练对话。
3. 普通成员看到的核心路径应始终是：

   `部门数字员工 -> 复制分身 -> 设置个人信息 -> 上岗 -> 平台会话立即可用`

4. IM 接入是**个人分身层面的可选操作**，在分身卡片上提供入口，不阻塞上岗流程。
5. 所有技术概念都由系统翻译为业务语言，用户不直接操作 manifest、ontology、skill、cli 等技术对象。

### 1.3 严格门槛

1. 部门版与私有分支都必须经过 AI 评估与人工评估，不能绕过门槛直接发布。
2. 评估失败后允许回退，但不允许以"跳过评估"的形式发布上线。
3. **例外：部门长快捷复制来的新部门员工，因继承源员工评估结果，可直接发布，无需重走评估。**
4. 部门员工发布后不配置 IM；IM 接入是个人分身层面的可选操作，不作为任何发布流程的必须条件。

### 1.4 边界清晰

1. Web 端负责雇佣、评估、配置与管理。
2. 数字员工上岗后支持两类主动发起对话的方式：
   - **平台会话**：在 Web 端内嵌界面直接对话，上岗即可用，无需任何额外配置。
   - **拉起 IM**：从 Web 端点击"去 {平台名}"按钮，跳转到对应 IM 应用并进入与员工机器人的私聊，需先完成 IM 注册。
3. 两种方式不互斥，底层共用同一运行时沙箱，对话上下文各自独立。

### 1.5 隐私优先

1. 部门长可以查看团队数字员工的状态、配置与元数据，但不能查看下属与数字员工的具体私聊内容。
2. 私有分支仅创建者本人可见、可评估、可放弃。
3. 所有页面字段展示都必须遵循：

   `租户隔离 + 用户隔离 + 角色隔离`

### 1.6 多渠道使用

1. 数字员工支持两类使用渠道：**站内对话（默认）** 和 **IM 接入（可选）**。
2. 站内对话：上岗即可用，无需额外配置。
3. IM 接入：支持飞书、钉钉、企微，每个平台独立配置，同一员工可同时接入多个平台。
4. IM 接入后仅支持一对一私聊，不提供群聊入口。

### 1.7 文案统一

1. 面向用户的主表达统一使用"雇佣"。
2. 推荐统一文案：

   雇佣、继续雇佣、重新雇佣、开始对话、配置 IM、去飞书使用、部门数字员工、我的数字员工

3. "快捷复制"作为功能入口文案，向用户强调"无需重新评估，直接发布"。

---

## 2. 角色、对象与状态

### 2.1 角色定义

- **部门长：** 负责查看企业模板池，发起部门版雇佣，**从已有部门员工快捷复制新员工**，查看部门数字员工，并对团队内处于评估中的实例继续处理直至上岗。
- **普通成员：** 负责查看部门数字员工中的已上岗实例，复制出个人分身，**通过站内对话直接使用**，并可按需接入 IM。
- **分身 Owner：** 指个人分身的拥有者。只有 owner 可以为自己的 personal_clone 创建私有分支。

### 2.2 核心对象

- **模板 template：** 雇佣起点，只能被浏览和选择，不能在雇佣端直接生产或派生新模板。
- **部门版 department：** 由部门长基于模板创建（正式雇佣）或基于已有部门员工快捷复制，面向本部门复用。前台展示文案建议为：**部门员工**。
- **个人分身 personal_clone：** 由用户基于部门版复制得到，独立使用，拥有自己的对外身份配置。前台展示文案建议为：**我的分身**。
- **私有分支 private_branch：** 由 personal_clone 的 owner 基于现有分身进行个性化定制得到，仅 owner 可见。前台展示文案建议为：**私人定制**。

### 2.3 状态定义

| 状态 | 说明 |
| --- | --- |
| `hired` | 已启动流程，尚未进入评估。 |
| `interning_ai` | AI 评估中。 |
| `interning_human` | 人工评估中。 |
| `live` | 已上岗，站内对话立即可用，可按需配置 IM。 |
| `failed` | 评估失败，等待进入 Review 决定回退位置。 |
| `retired` | 已退役，只读。 |

### 2.4 状态与模块映射

`部门数字员工` 采用统一模块名，但按角色展示不同内容：

**部门长视图：**

1. 已雇佣：对应 `hired`
2. 待实习：包含两个子标签
   - AI 评估，对应 `interning_ai`
   - 人工评估，对应 `interning_human`
3. 已上岗：对应 `live`

**普通成员视图：**

1. 只显示已上岗
2. 只展示当前部门可复制的 `live` 部门版

**补充说明：**

1. `failed` 不单独做一级模块，优先通过实例详情页和 Review 页承接。
2. `retired` 不进入部门主列表，优先放在"我的数字员工"中作为历史状态承接。

### 2.5 状态与按钮联动

| 状态 | 按钮 |
| --- | --- |
| `live`（我的分身/私人定制） | **开始对话**（主按钮）、配置 IM、详情 |
| `live`（部门员工 + 部门长） | 快捷复制、重新雇佣、复制给自己 |
| `live`（部门员工 + 普通成员） | 创建分身 |
| `interning_ai` / `interning_human` | 查看评估进度 |
| `failed` | 查看评估报告、继续雇佣 |
| `retired` | 查看历史、重新复制 或 重新雇佣 |

### 2.6 权限边界

1. 部门长可以查看本部门范围内的部门版状态、评估进度和上岗情况。
2. 部门长的"我的数字员工"仅展示其本人拥有的"我的分身"和"私人定制"。
3. 普通成员在"部门数字员工"中只能看本部门 `live` 的"部门员工"，以及自己的"我的分身"和"私人定制"。
4. 非 owner 不展示站内对话入口和会话历史入口。
5. 私有分支详情仅 owner 可见。

---

## 3. 信息架构

### 3.1 顶层模块

**部门长模块：**

1. 企业模板池
2. 部门数字员工
3. 我的数字员工

**普通成员模块：**

1. 部门数字员工
2. 我的数字员工

### 3.2 页面分类

**列表 / 模块页：**

1. 企业模板池
2. 部门数字员工
3. 我的数字员工

**详情页：**

1. 数字员工详情
2. 模板详情

**流程页：**

1. 部门版雇佣页
2. AI 评估页
3. 人工评估页
4. Review 页
5. 上岗页（评估通过后确认上岗，IM 配置为可选步骤）
6. 分身复制向导
7. 私有分支定制页
8. **部门长快捷复制确认页（新增）**
9. **IM 配置页（多平台，新增）**

**对话页：**

1. **站内对话页（新增，主要对话渠道）**

**共享组件：**

1. IM 配置引导弹层（支持飞书/钉钉/企微）

### 3.3 核心入口策略

1. 部门长从"企业模板池"进入完整雇佣流程。
2. 部门长从"部门数字员工"的 `live` 员工操作菜单进入**快捷复制**流程。
3. 部门长从"部门数字员工"管理本部门的所有状态实例。
4. 部门长从"我的数字员工"管理自己拥有的"我的分身"和"私人定制"。
5. 普通成员从"部门数字员工"的已上岗视图进入三步复制流程。
6. 用户从"我的数字员工"的员工卡片**直接进入站内对话**（主要使用入口）。
7. 数字员工详情页承担所有状态承接与动作分发，包括 IM 配置入口。

---

## 4. 页面清单与详细说明

### 4.1 企业模板池

- **页面目标：** 供部门长浏览企业模板和全局模板，并选择一个模板开始雇佣部门版。
- **适用角色：** 部门长
- **进入条件：** 从顶层模块进入

**核心模块：**

1. 搜索区
2. 模板列表区

**卡片字段：**

1. 模板名称
2. 模板来源：全局通用 / 企业专属
3. 场景简介
4. 适用部门
5. 使用次数

**关键规则：**

1. 不提供"派生新模板"、"编辑模板源码"等入口。
2. 不提供新增模板入口，部门长只能使用已有模板发起部门版雇佣。
3. 用户只能基于模板创建部门版，不能在雇佣端生产模板。

---

### 4.2 部门数字员工

- **页面目标：** 以统一模块名承接部门级数字员工资产，但根据角色提供不同视图。
- **适用角色：** 部门长、普通成员
- **进入条件：** 从顶层模块进入

**部门长视图：**

1. 一级标签：已雇佣、待实习、已上岗
2. 二级标签：待实习下拆分为 AI 评估、人工评估
3. 列表区：按卡片展示团队内数字员工

**普通成员视图：**

1. 不显示"已雇佣"、"待实习"
2. 仅显示"已上岗"结果集
3. 本质上是本部门可复制的 `live` 部门版列表

**卡片字段建议：**

1. 对外名称与头像
2. 当前状态或当前阶段
3. 最近更新时间
4. 查看详情

**动作规则：**

- 部门长 / 全部状态：主动作统一为查看详情
- 部门长 / 已上岗：操作菜单新增**"快捷复制"**入口
- 普通成员 / 已上岗：主动作统一为查看详情

**关键规则：**

1. 这是部门级数字员工的统一主入口。
2. 卡片主动作统一为查看详情，由详情页承接不同状态下的下一步按钮。
3. "同名模块，不同角色看不同状态范围"是本页核心设计。
4. `failed` 实例不新增一级标签，通过实例详情与 Review 页回退承接。
5. 该页不承接个人私有分支的详细管理，个人资产统一去"我的数字员工"。

---

### 4.3 我的数字员工

- **页面目标：** 按个人视角管理当前用户本人拥有的数字员工实例，并提供站内对话快速入口。
- **适用角色：** 部门长、普通成员
- **进入条件：** 从顶层模块进入

**页面范围：**

1. 仅显示当前用户本人拥有的"我的分身"和"私人定制"。
2. 不在此页展示整个团队的"部门员工"列表。

**核心模块：**

1. 筛选条：状态、实例类型、来源模板、最近活跃时间
2. 列表区：卡片化展示数字员工
3. 汇总区：实例总数、状态分布、异常实例数

**卡片字段建议：**

1. 对外名称与头像
2. 实例类型标签：我的分身 / 私人定制
3. 当前状态标签
4. 来源关系
5. 最近活跃时间
6. IM 配置状态：飞书 / 钉钉 / 企微各自的配置图标（已配置绿色、未配置灰色、异常红色）

**动作规则：**

| 状态 | 主动作 | 次要动作 |
| --- | --- | --- |
| `live` | **开始对话**（进入平台会话） | **去 IM**（拉起 IM，已注册时显示）、配置 IM、详情 |
| `interning_ai` / `interning_human` | 查看评估进度 | — |
| `failed` | 查看报告、继续雇佣 | — |
| `retired` | 查看历史 | 重新复制 |

**关键规则：**

1. `live` 状态卡片的**主按钮统一为"开始对话"**，点击直接进入平台会话页，不跳转到详情页。
2. **"去 IM"按钮（拉起 IM）**：至少有一个 IM 平台已注册且连接成功时显示；点击后若已配置多个平台，弹出平台选择气泡（图标列表）；若只有一个，直接跳转拉起对应 IM。
3. **"配置 IM"入口**：始终可见，供用户随时进入 IM 配置页。
4. 点击"去 IM"但尚未配置任何 IM 平台时：显示提示"该员工尚未接入 IM，是否立即配置？"，提供"去配置"和"取消"。

---

### 4.4 数字员工详情

- **页面目标：** 承接单个实例的状态、来源、身份、运行情况与后续动作。
- **适用角色：** 全体用户
- **进入条件：** 从列表页、评估完成页进入

**核心模块：**

1. **基本信息区：** 名称、头像、类型、状态、所属部门、owner
2. **来源关系区：** 模板 → 部门员工 → 我的分身 → 私人定制（快捷复制来源的员工标注"来自快捷复制"）
3. **能力简介区：** 业务可读能力标签，不展示底层文件
4. **IM 接入区（仅我的分身 / 私人定制显示，部门员工不显示）：**
   - 已连接：显示平台图标 + 绿色"已连接"标签 + "去 {平台名}" 按钮（拉起 IM）
   - 未配置：显示平台图标 + 灰色"未配置"标签 + "配置 IM" 按钮
   - 配置异常：显示平台图标 + 红色"配置异常"标签 + "重新配置" 按钮
5. **运行状态区：** 最近活跃时间、累计对话轮次、最近错误
6. **操作区：** 根据类型与状态显示主动作

**主按钮建议：**

| 实例类型 + 状态 + 角色 | 主按钮 | 次要按钮 |
| --- | --- | --- |
| 部门员工 + `hired` | 继续雇佣 | — |
| 部门员工 + `interning_ai` | 查看 AI 评估 | — |
| 部门员工 + `interning_human` | 查看人工评估 | — |
| 部门员工 + `live` + 部门长 | 重新雇佣 | **快捷复制**、复制一个给自己 |
| 部门员工 + `live` + 普通成员 | 创建分身 | — |
| 我的分身 + `live` | **开始对话** | 创建私有分支、退役 |
| `failed` | 查看评估报告 | 继续雇佣 |
| `retired` | 查看历史 | 重新复制 / 重新雇佣 |

**关键规则：**

1. 非 owner 不显示"开始对话"和会话历史入口。
2. 部门长查看团队内实例时，只展示元数据、状态和配置，不展示私聊明细。
3. 详情页应根据当前实例状态提供进入下一步环节的主按钮。
4. 私有分支详情仅 owner 可见。

---

### 4.5 模板详情

- **页面目标：** 帮助部门长判断模板是否适合当前部门，并作为唯一的部门版雇佣起点。
- **适用角色：** 部门长
- **进入条件：** 从企业模板池卡片进入

**核心模块：**

1. 模板概览：名称、来源、适用场景、适配度提示
2. 业务描述
3. 能力清单
4. 使用情况：已生成部门版数、已被复制次数
5. 主动作区：雇佣

**关键规则：**

1. 从此页发起雇佣时要带入 `template_id`。
2. 关闭或返回时保留模板池搜索关键词和滚动位置。

---

### 4.6 部门版雇佣页

- **页面目标：** 通过 `digital_employee_discovery` 引导部门长完成完整六步雇佣流程。
- **适用角色：** 部门长
- **进入条件：** 从模板详情点击"雇佣"进入

---

#### 4.6.1 整体布局

```
┌─────────────────────────────────────────────────────────────┐
│  顶部引导步骤条（始终固定，包含 discovery 的 6 个步骤）：        │
│  场景匹配 → 差距挖掘 → 人设生成 → 知识生成 → 能力生成 → 外部对接  │
├────────────────────────────┬────────────────────────────────┤
│                            │                                │
│      左侧：会话区           │       右侧：产物面板            │
│      （始终固定）           │       （每步均有产出物）         │
│                            │                                │
│  · Bot 引导对话             │  Steps 1–2：当前步骤产出物      │
│  · 用户自由输入             │    bot 生成后展示，用户逐项确认  │
│  · 文件上传入口             │                                │
│    （知识材料 / 背景文档）   │  Step 2 完成后追加：            │
│                            │    Todo List（全局聚合视图）     │
│                            │    后续各步产出物追加到此列表    │
└────────────────────────────┴────────────────────────────────┘
│  底部操作区：保存退出 · 上一步 · 下一步（当前步骤确认后激活）  │
└─────────────────────────────────────────────────────────────┘
```

---

#### 4.6.2 顶部引导步骤条与右侧产物面板

**顶部引导步骤条（始终固定）**

步骤条始终显示在整个界面最顶部，包含 discovery 的 6 个步骤，以步骤条形式呈现，告知用户当前所处阶段与整体进度：

| 步骤 | 步骤条说明文字 |
|---|---|
| 场景匹配 | 描述业务场景：触发事件、当前处理方式、最难判断是什么 |
| 差距挖掘 | 找出模板与场景之间的差距：现有流程、常见异常、必须懂的业务规则 |
| 人设生成 | 确认数字员工是谁：Bot 生成人设草稿，逐项确认或修改 |
| 知识生成 | 补充员工所需知识材料：内部文档、规范、FAQ、历史案例 |
| 能力生成 | 定义员工能做什么：通过对话生成，或上传已有 Skill 文件 |
| 外部对接 | 确认需要连接的外部系统：系统名称、读写方向、认证方式 |

**右侧产物面板：每步均有产出物**

右侧面板在每一步都展示当前步骤的产出物，bot 生成后即展示，用户逐项确认。前两步（场景匹配、差距挖掘）的产出物各自独立展示在右侧面板中。

差距挖掘（Step 2）完成后，系统基于识别出的差距自动生成全局 Todo List，追加显示在右侧面板，贯穿后续所有步骤；后续各步的产出物逐步填入 Todo List，不是一次性全部出现。

---

#### 4.6.3 Todo List 结构

Todo List 按 4 个 sub-skill 分组，每个 sub-skill 的输出对应一组待确认条目：

```
□ 人设档案  ── manifest-skill 输出
  □ 员工名称
  □ 使命陈述
  □ 负责范围（展开显示每条）
  □ 自主权级别
  □ 绝不能事项

□ 本体切片  ── ontology-skill 输出
  □ 实体清单（N 个实体，逐一确认）
  □ 动作清单（N 个动作）
  □ 约束规则（N 条，约束合规类标红高亮）

□ 能力清单  ── generator-skill 输出
  □ 能力：{name} — {description}
  □ 能力：{name} — {description}
  □ ...

□ 外部接口  ── cli-skill 输出
  □ {系统名}：{调用签名} 待确认
  □ {系统名}：{调用签名} 待确认
  □ ...
```

**条目状态：**

| 状态 | 样式 | 说明 |
|---|---|---|
| 待生成 | `○ 灰色` | Sub-skill 尚未运行到此条目 |
| 待确认 | `□ 正常` | Sub-skill 已生成，等待用户确认 |
| 已确认 | `✓ 绿色` | 用户已确认，内容锁定 |
| 已修改待重确认 | `□ 橙色` | 自由补充模式下用户修改了对应字段，需重新确认 |
| 已废弃 | `~~删除线~~` | 用户主动标记为不需要或被替换 |

用户点击任意条目 → 右侧展开该条目的完整输出内容 → 用户可确认（打勾）或在会话框输入修改意见。

---

#### 4.6.4 能力生成步骤的特殊交互（generator-skill）

能力生成步骤支持两种并行的输入模式：

**会话生成（右侧 Todo List 中的"通过对话生成"）**
- 用户在左侧会话框描述一项能力需求
- Bot 识别后生成 Skill 草稿，包含基本字段：
  - `name`：技能名称（英文 slug + 中文标签）
  - `description`：用途描述（这个技能做什么、什么时候触发）
  - `trigger`：触发条件
  - `autonomy`：自主权级别
- 草稿出现在右侧 Todo List 对应条目下，状态为"待确认"
- 用户确认或在会话框追加修改意见

**上传 Skill 文件（右侧面板上传区）**
- "上传 Skill 文件"入口仅在 Todo List 的「能力清单」分组内展示，其余分组及其他步骤均不显示
- 用户上传文件后，系统解析并提取 `name`、`description`、`trigger`、`autonomy` 字段
- 解析结果展示在右侧面板，缺失字段高亮提示
- 解析通过后自动追加到 Todo List，状态为"待确认"
- 两种方式的条目格式完全一致，合并显示在同一列表中

**与会话框文件上传的区别：**

| 上传位置 | 上传内容 | 用途 |
|---|---|---|
| 会话框（左侧输入区） | 知识文档、流程材料、背景资料 | Bot 读取作为对话上下文，影响提炼结果 |
| 右侧面板上传区 | Skill 定义文件 | 系统解析，直接转为 Todo 条目待确认 |

---

#### 4.6.5 关键规则

1. 左侧会话区始终固定，不随步骤切换消失。
2. 顶部引导步骤条始终固定在界面最顶部，右侧面板在每一步均展示该步产出物；Step 2 完成后右侧追加全局 Todo List，两者同属右侧产物面板，不是两个独立页面。
3. 用户不直接操作底层技术对象，所有字段通过 bot 对话或面板确认完成。
4. Todo List 在 Step 2 差距挖掘完成后生成，后续步骤的条目随 sub-skill 执行逐步填入，不是一次性全部出现。
5. 每个条目必须经过用户显式确认（打勾）才算完成，bot 生成不等于确认。
6. 支持保存退出，后续从"部门数字员工 > 已雇佣"恢复，Todo List 状态完整保留。
7. 六步全部确认后，系统将已沉淀的中间产物汇总为完整实例配置，再进入 AI 评估。
8. **首次完整通过前：严格顺序，不允许跳步。** 顶部步骤条中，未解锁的步骤置灰，"下一步"按钮仅在当前步骤所有 Todo 条目已确认后激活。
9. **首次完整通过后：进入自由补充模式。** 顶部步骤条所有步骤可点击跳转；会话框始终可输入；修改某条目后该条目状态变为"已修改待重确认"；产物区顶部显示"有未汇总的修改"，用户点击"完成修改"后重新汇总。

---

### 4.7 AI 评估页

- **页面目标：** 作为独立功能承接 AI 自动化评估，展示标准检查、测试执行、判分与训练建议结果。
- **适用角色：** 部门长、私有分支用户
- **进入条件：** 雇佣配置完成后自动进入，或从列表页查看

**核心模块：**

1. 评估进度区：当前阶段、已完成项、预计剩余时间
2. 标准检查区：结构完整性、边界配置、资料完整度、对接校验
3. 测试结果区：场景用例、得分、失败案例、风险说明
4. 训练建议区：对下一步人工评估的重点提示
5. 结论区：通过 / 不通过 / 待补充

**评估启动前的衔接状态：**

进入 AI 评估页后，系统先执行测试用例衔接检查，再正式启动评估。页面呈现三种前置状态：

| 状态 | 显示内容 | 用户操作 |
|---|---|---|
| 检查中 | "正在准备测试用例…" + 进度指示 | 等待（通常 < 3 秒） |
| 检查通过（正常路径） | 直接进入评估进度，无额外提示 | 无需操作 |
| 补充生成中（异常路径） | "雇佣阶段未能生成本体测试用例，正在补充生成…" + 进度指示 | 等待；可选择跳过（仅使用模板通用测试集） |
| 补充生成失败 | 黄色提示条："本次评估仅使用模板通用测试集，建议完成雇佣流程后重新评估" | 可继续评估或返回雇佣流程 |

**关键规则：**

1. AI 评估通过后才允许进入人工评估。
2. AI 评估不直接完成上岗决策。
3. 衔接检查对正常路径用户完全无感，仅在异常路径下展示提示。

---

### 4.8 人工评估页

- **页面目标：** 承接真实用户评估，在场景轮次会话中完成最终上岗判断。
- **适用角色：** 部门长、私有分支 owner
- **进入条件：** AI 评估通过后进入

**核心模块：**

1. 场景任务区：按轮次展示测试任务
2. 会话预览区：查看思考链路、工具调用、对话详情
3. 综合报告区：汇总所有场景后的整体判断
4. 上岗操作区：确认上岗、修改配置后重评

**主流程按钮：**

- 人工评估通过 → 确认发布（进入发布确认页）
- 人工评估不通过 → Review / 返回雇佣

**关键规则：**

1. 用户不能跳过人工评估直接上岗。
2. 若存在不合格或未评估场景，强制上岗必须带二次确认。

---

### 4.9 Review 页

- **页面目标：** 在评估失败后帮助用户决定回退到哪个工位继续雇佣配置。
- **适用角色：** 部门长、私有分支用户
- **进入条件：** AI 评估或人工评估失败

**核心模块：**

1. 失败摘要区：总分、低分维度、关键失败案例
2. 回退入口区：场景匹配、差距挖掘、人设生成、知识生成、能力生成、外部对接
3. 保留说明区：说明已上传原料保留、生成结果将被重算
4. 风险提示区：连续失败次数与系统建议
5. 放弃区：仅私有分支显示"放弃私有分支"

**关键规则：**

1. 部门版不提供"放弃本次雇佣"的主出口，只能继续完成或暂存。
2. 私有分支允许放弃，且不能影响原分身继续可用（IM 路由恢复至原分身，站内对话恢复至原分身）。

---

### 4.10 发布确认页

- **页面目标：** 评估通过后，部门长确认将部门员工发布到"部门数字员工"列表，供成员复制使用；个人分身的上岗也在此完成。
- **适用角色：** 部门长（发布部门员工）/ 全体用户（个人分身上岗）
- **进入条件：** 人工评估通过后进入；分身复制向导最后一步也复用此页

**部门员工发布流程：**

1. **发布确认区：** 展示员工信息（display_name、头像）与"发布到部门员工列表"按钮
2. **发布说明：**
   - "发布后该员工将出现在部门数字员工列表，部门内成员均可看到"
   - "成员可复制分身后在飞书/钉钉/企微中使用，或直接通过平台会话与分身对话"
   - "部门员工本身不配置 IM，IM 接入由成员在各自的分身上完成"
3. **发布进度区：** 点击后显示：沙箱启动、状态切换为 `live`、员工出现在部门列表
4. **完成后引导：**
   - "员工已发布！建议您先复制一个自己的分身来体验"
   - 主按钮："立即创建我的分身"（跳转分身复制向导）
   - 次要链接："稍后再说"（返回部门员工列表）

**个人分身上岗流程（分身复制向导复用此步）：**

1. **上岗确认区：** 展示分身信息（display_name、头像）与"确认上岗"按钮，说明"上岗后平台会话立即可用"
2. **上岗进度区：** 沙箱启动、状态切换为 `live`
3. **完成后引导：**
   - 主按钮："立即开始对话"（进入平台会话页）
   - 次要链接："配置 IM"（进入 IM 配置页，可选）

**关键规则：**

1. 部门员工发布**不包含任何 IM 配置步骤**。
2. 个人分身上岗后 IM 配置为可选，随时可在分身详情页或"我的数字员工"卡片上发起。
3. `display_name` 在同一类型（部门员工 / 个人分身）范围内必须唯一，提交前校验。

---

### 4.11 分身复制向导

- **页面目标：** 用最少步骤完成普通成员创建个人分身，也就是从部门员工到个人分身的复制与上岗。
- **适用角色：** 普通成员，也可供部门长复制给自己
- **进入条件：** 从"部门数字员工"的已上岗视图或部门员工详情点击"创建分身"

**建议采用两步横向向导：**

1. **确认复制：** 展示母版简介与"复制后独立"说明，明确告知"不进入双阶段评估"
2. **配置个人信息并上岗：** 填写 `display_name`（必填）、`display_avatar`（可选）、`display_description`（可选），确认后系统自动创建分身并上岗

**完成后：**

- 显示"你的分身已上岗！站内对话已就绪"
- 提供"立即开始对话"主按钮（进入站内对话页）
- 提供"配置 IM"次要链接（进入 IM 配置页，可选）

**页面要求：**

1. 不出现雇佣教练对话。
2. 总操作步数不超过 2 步。
3. 明确说明"不进入双阶段评估，继承部门员工的评估结果"。
4. 只保留当前步骤必要字段，不引入复杂可选项。
5. IM 配置不在向导内展开，通过完成后的引导链接进入。

---

### 4.12 私有分支定制页

- **页面目标：** 在不破坏原分身的前提下完成个性化定制，发布到个人数字员工分支，仅自己可见。
- **适用角色：** 分身 owner
- **进入条件：** 从 `personal_clone` 详情点击"创建私有分支"

**交互设计直接复用 §4.6 部门版雇佣页的六步流程**，不内嵌额外逻辑。与部门版雇佣的两处差异：

| 维度 | 部门版雇佣（§4.6） | 私有分支定制（§4.12） |
|---|---|---|
| 起点 | 企业模板 | 用户已有的个人分身 |
| 发布目标 | 部门数字员工列表（全部门可见） | 个人数字员工分支（**仅自己可见**） |

六步流程、左侧会话区、右侧引导面板 / Todo List、sub-skill 输出逐项确认、文件上传交互，均与 §4.6 一致，此处不重复描述。

**发布后：**

- 私有分支出现在"我的数字员工"列表，标签为"私人定制"
- 评估通过后不新增 IM 机器人，**复用原分身已配置的各平台 IM 凭证，将各平台消息路由切换到私有分支实例**
- 站内对话自动切换到私有分支版本

**关键规则：**

1. 私有分支不能再创建二级分支。
2. 定制失败可放弃，路由恢复至原分身（IM 和站内对话均恢复），不影响原分身继续可用。
3. 私有分支详情及对话内容仅 owner 可见，不外溢。

---

### 4.13 部门长快捷复制确认页（新增）

- **页面目标：** 引导部门长从已有部门员工一键复制出新的部门员工，跳过评估直接上岗。
- **适用角色：** 部门长
- **进入条件：** 从"部门数字员工"列表卡片操作菜单或员工详情页点击"快捷复制"

**页面结构（建议用弹窗或侧边栏呈现，操作轻量）：**

1. **源员工信息区：** 展示被复制的员工名称、头像、简介
2. **说明文案：**
   - "将基于 `{源员工名称}` 创建新的部门员工"
   - "新员工继承源员工的评估结果，无需重新评估，创建后直接上岗"
   - "创建后两个员工相互独立，修改任意一个不影响另一个"
3. **新员工信息填写区：**
   - `display_name`（必填）：新员工的部门内显示名，不能与现有部门员工重名
   - `display_avatar`（可选）：默认沿用源员工头像，可更换
   - `display_description`（可选）
4. **确认按钮：** "创建并发布"

**完成后：**

- 显示"新部门员工已上岗！"
- 提供"立即复制一个分身来体验"按钮（跳转至分身复制向导，预填充来源为刚创建的新员工）
- 提供"稍后再说"（返回部门员工列表）

**关键规则：**

1. 只有 `live` 状态的部门员工可以作为快捷复制的来源，其他状态入口置灰。
2. 快捷复制不能来源于个人分身（personal_clone），只能来源于部门员工（department）。
3. `display_name` 在当前部门的 `live` 员工中必须唯一，实时校验。
4. 完成后必须主动引导部门长创建分身体验（而不是沉默跳回列表）。

---

### 4.14 IM 配置页（新增）

- **页面目标：** 引导用户选择接入模式、填写对应凭证、点击注册并查看连接状态，将数字员工绑定到外部 IM 机器人。
- **适用角色：** 全体用户
- **进入条件：** 从"我的数字员工"卡片"配置 IM"、数字员工详情页 IM 接入区、上岗页 IM 接入引导区进入

---

**页面顶层结构：**

1. **平台选择 Tab：** 飞书 | 钉钉 | 企微（各自独立配置，Tab 上显示当前配置状态图标）
2. **当前状态提示：** 未配置 / 已连接 / 配置异常

每个平台 Tab 内部结构相同，分为：**描述文字 → 接入模式切换 → 凭证表单 → 注册按钮 → 连接状态展示**

---

**飞书 Tab：**

**描述文字：**
> 输入飞书应用凭据以将此数字员工绑定到飞书机器人。App ID 和 App Secret 为必填项。

**接入模式切换（单选）：**

```
● WebSocket 长连接（推荐）    ○ 使用 URL 回调
```

**WebSocket 长连接 - 凭证表单：**

| 字段 | 必填 | 说明 |
|---|---|---|
| App ID | 是 | 飞书自建应用的 app_id |
| App Secret | 是 | 遮蔽显示，飞书自建应用的 app_secret |

**URL 回调 - 凭证表单（切换后显示）：**

额外展示该实例专属 Webhook URL（一键复制）：
`https://{platform}/im/feishu/webhook/{instance_id}`

| 字段 | 必填 | 说明 |
|---|---|---|
| App ID | 是 | 飞书自建应用的 app_id |
| App Secret | 是 | 遮蔽显示 |
| Encrypt Key | 是 | 遮蔽显示，消息加密密钥 |
| Verification Token | 否 | 遮蔽显示，Webhook 签名校验 |

**配额提示（URL 回调模式）：**
> 注意：飞书企业自建应用存在数量配额，请确认贵企业配额充足。

---

**钉钉 Tab：**

**描述文字：**
> 输入钉钉机器人凭据以将此数字员工绑定到钉钉机器人。App ID 和 App Secret 为必填项。

**接入模式切换（单选）：**

```
● WebSocket 长连接（推荐）    ○ 使用 URL 回调
```

**WebSocket 长连接 - 凭证表单：**

| 字段 | 必填 | 说明 |
|---|---|---|
| App ID | 是 | 钉钉应用的 ClientID（App Key） |
| App Secret | 是 | 遮蔽显示 |

**URL 回调 - 凭证表单（切换后显示）：**

额外展示 Webhook URL（一键复制）：
`https://{platform}/im/dingtalk/webhook/{instance_id}`

| 字段 | 必填 | 说明 |
|---|---|---|
| App ID | 是 | 钉钉应用的 ClientID |
| App Secret | 是 | 遮蔽显示 |
| Encrypt Key | 是 | 遮蔽显示，消息加密密钥 |
| Token | 否 | 遮蔽显示，签名校验 Token |
| AES Key | 否 | 遮蔽显示，消息体 AES 解密密钥 |

---

**企微 Tab：**

**描述文字：**
> 选择连接方式并输入对应凭据，以将此数字员工绑定到企微 AIBot。

**接入模式切换（单选）：**

```
● WebSocket 长连接（推荐）    ○ 使用 URL 回调
```

**WebSocket 长连接 - 凭证表单：**

| 字段 | 必填 | 说明 |
|---|---|---|
| App ID | 是 | 企微应用的 AgentID |
| App Secret | 是 | 遮蔽显示，企微应用的 Secret |

**URL 回调 - 凭证表单（切换后显示）：**

额外展示 Webhook URL（一键复制）：
`https://{platform}/im/wecom/webhook/{instance_id}`

| 字段 | 必填 | 说明 |
|---|---|---|
| Token | 是 | 遮蔽显示，消息签名校验 Token |
| EncodingAESKey | 是 | 遮蔽显示，消息体 AES 解密密钥 |

---

**注册按钮与连接状态（三平台通用）：**

1. **"注册"按钮**（主操作按钮，点击后发起连接）
   - 点击后按钮变为 loading 状态，显示"连接中..."
   - 同时展示连接状态区

2. **连接状态展示区：**

| 状态 | 展示 |
|---|---|
| 连接中 | 旋转图标 + "正在建立连接..." |
| 连接成功 | 绿色勾 + "连接成功，可以开始使用了" |
| 连接失败 | 红色叉 + 错误原因（如"App Secret 有误，请检查后重试"）+ "重新配置"按钮 |

3. **连接成功后：**
   - Tab 上的状态图标变为绿色"已连接"
   - 显示"去 {平台名}" 快捷跳转链接
   - 提供"解除绑定"链接（点击需二次确认）

---

**通用规则：**

1. 各平台配置互相独立，注册一个不影响其他平台。
2. 凭证字段输入时遮蔽显示（`****`），已保存后不展示明文，仅显示"已配置"。
3. 切换接入模式时，表单字段动态变化，已填内容清空。
4. 注册失败时展示具体错误原因，允许修改后重新注册。
5. 已连接状态下可重新注册（更新凭证），需二次确认。

---

### 4.15 平台会话页（新增）

- **页面目标：** 提供平台内置的对话界面，让用户无需任何额外配置、无需跳转外部 IM，直接在 Web 端与数字员工对话。
- **适用角色：** 实例 owner
- **进入条件：** 从"我的数字员工"卡片点击"开始对话"，或从数字员工详情点击"开始对话"

**页面结构：**

1. **顶部信息栏：**
   - 员工 display_name + display_avatar，实例状态标签（已上岗）
   - **"去 IM"快捷入口**（右侧）：已注册 IM 时显示对应平台图标，点击直接拉起对应 IM；未注册时显示"接入 IM"链接
2. **消息列表区：**
   - 用户消息（右对齐）
   - 员工回复（左对齐），支持流式输出（逐字显示）
   - 时间戳（按时间段分组显示）
   - 回复生成中显示加载动画
3. **输入区：**
   - 文本输入框（支持 Shift+Enter 换行，Enter 发送）
   - 发送按钮
   - 清空历史按钮（弹出二次确认）

**历史记录：**

- 默认展示最近 50 条消息，支持上拉加载更多
- 历史持久化保存，重新进入可继续上次平台会话
- 清空历史仅清空平台会话记录，不影响 IM 渠道的聊天记录

**拉起 IM 交互（顶部"去 IM"区域）：**

| 场景 | 展示 |
| --- | --- |
| 已注册 1 个 IM 平台 | 显示该平台图标，点击直接跳转拉起 IM |
| 已注册多个 IM 平台 | 显示多个平台图标，点击任意图标跳转对应 IM |
| 未注册任何 IM | 显示"接入 IM"文字链接，点击进入 IM 配置页 |

**关键规则：**

1. 仅 `live` 状态的实例才展示完整对话界面，其他状态展示占位提示（如"该员工尚未上岗"）。
2. 对话历史按 `instance_id + user_id` 严格隔离，他人不可见。
3. 平台会话与 IM 渠道各自独立的上下文，不互通。
4. "去 IM"拉起的是外部 IM 应用，跳转后用户在 IM 中进行的对话不显示在本页历史记录中。

---

## 5. 关键流程

### 5.1 部门长主流程（正式雇佣）

```
企业模板池
  -> 模板详情
  -> 部门版雇佣页
  -> AI 评估页
  -> 人工评估页
  -> 发布确认页（点击"发布到部门员工列表"）
  -> 部门数字员工列表（员工出现，成员可见可复制）
  -> [部门长自己] 立即创建我的分身 -> 分身复制向导 -> 平台会话
```

**续办逻辑：**

1. 部门数字员工 / 已雇佣 -> 数字员工详情 -> 继续雇佣 -> 部门版雇佣页
2. 部门数字员工 / 待实习 / AI 评估 -> 数字员工详情 -> 查看 AI 评估 -> AI 评估页
3. 部门数字员工 / 待实习 / 人工评估 -> 数字员工详情 -> 查看人工评估 -> 人工评估页
4. 部门数字员工 / 已上岗 -> 数字员工详情

**失败回路：**

```
AI 评估失败或人工评估失败
  -> Review 页
  -> 回退到对应雇佣工位继续处理
```

---

### 5.2 部门长快捷复制流程（新增）

```
部门数字员工（已上岗）
  -> 员工卡片操作菜单 "快捷复制"
  -> 快捷复制确认页
     填写新员工 display_name（必填）
     [可选] 更换头像、描述
     点击"创建并发布"
  -> 新部门员工直接上岗（status=live）
  -> 完成引导："建议先复制一个分身来体验"
     [立即创建我的分身] -> 分身复制向导
     [稍后再说] -> 部门员工列表
```

---

### 5.3 普通成员主流程

```
部门数字员工（已上岗视图）
  -> 数字员工详情
  -> 分身复制向导（2步）
     步骤1：确认复制
     步骤2：设置 display_name，确认上岗
  -> 完成：分身已上岗
     [立即开始对话] -> 平台会话页（默认渠道，无需配置）
     [配置 IM]      -> IM 配置页（可选，配置后可拉起 IM）

平台会话页
  -> 直接在 Web 端与数字员工对话
  -> 顶部 [去 IM] -> 拉起对应 IM 应用，跳转进入 IM 私聊（需已注册）
  -> 顶部 [接入 IM] -> IM 配置页（未注册时显示）
```

---

### 5.4 IM 接入流程（可选，任意时机）

```
我的数字员工（卡片）或 数字员工详情
  -> 点击"配置 IM"
  -> IM 配置页
     选择平台 Tab（飞书/钉钉/企微）
     复制 Webhook URL
     去对应平台后台配置机器人
     回填凭证
     点击"注册"
     连接成功 -> IM 接入完成，"去 {平台名}" 按钮出现
```

---

### 5.5 私有分支流程

```
数字员工详情
  -> 私有分支定制页
  -> AI 评估页
  -> 人工评估页
  -> IM 路由切换（复用原分身各平台 IM 凭证）
     站内对话切换到私有分支
  -> 私有分支详情
```

**失败回路：**

```
AI / 人工评估失败
  -> Review 页
  -> 回退定制工位
或
  -> 放弃私有分支
  -> 返回原分身详情（IM 路由恢复至原分身，站内对话恢复至原分身）
```

---

## 6. 页面跳转矩阵

| 来源页面 | 触发动作 | 目标页面 | 条件 |
| --- | --- | --- | --- |
| 企业模板池 | 查看模板详情 | 模板详情 | 部门长 |
| 模板详情 | 雇佣 | 部门版雇佣页 | 部门长 |
| 部门数字员工 / 已雇佣 | 查看详情 | 数字员工详情 | 当前实例状态为 `hired` |
| 数字员工详情 | 继续雇佣 | 部门版雇佣页 | 当前实例状态为 `hired`，且实例类型为部门员工 |
| **部门数字员工 / 已上岗** | **快捷复制** | **快捷复制确认页** | **部门长，且实例为 `live` 部门员工** |
| **快捷复制确认页** | **创建并发布** | **部门员工列表（已上岗）** | **填写完成，display_name 不重名** |
| 部门版雇佣页 | 六步确认完成 | AI 评估页 | 全部步骤已确认 |
| 部门数字员工 / 待实习 / AI 评估 | 查看详情 | 数字员工详情 | 当前实例状态为 `interning_ai` |
| 数字员工详情 | 查看 AI 评估 | AI 评估页 | 当前实例状态为 `interning_ai` |
| AI 评估页 | 进入人工评估 | 人工评估页 | AI 评估通过 |
| AI 评估页 | 去 Review | Review 页 | AI 评估失败 |
| 部门数字员工 / 待实习 / 人工评估 | 查看详情 | 数字员工详情 | 当前实例状态为 `interning_human` |
| 数字员工详情 | 查看人工评估 | 人工评估页 | 当前实例状态为 `interning_human` |
| 人工评估页 | 确认发布 | 发布确认页 | 人工评估通过（部门员工） |
| 人工评估页 | 确认上岗 | 发布确认页 | 人工评估通过（私有分支） |
| 人工评估页 | 打回 | Review 页 | 人工评估失败 |
| 发布确认页（部门员工） | 发布到部门员工列表 | 部门数字员工列表 | status=live，成员可见 |
| 发布确认页（部门员工）完成后 | 立即创建我的分身 | 分身复制向导 | 部门长体验引导 |
| 发布确认页（个人分身） | 确认上岗 | 平台会话页 | status=live |
| 部门数字员工 / 已上岗 | 查看详情 | 数字员工详情 | 当前实例状态为 `live` |
| 数字员工详情 | 创建分身 | 分身复制向导 | 当前角色为普通成员，且实例为 `live` 部门员工 |
| 分身复制向导 | 完成复制上岗 | 完成页（提供"开始对话"按钮） | 实例状态切换为 `live` |
| **完成页 / 我的数字员工卡片** | **开始对话** | **平台会话页** | **当前实例 status=live，且用户为 owner** |
| **我的数字员工卡片 / 数字员工详情** | **去 IM（拉起 IM）** | **对应 IM 应用（外部跳转）** | **至少一个 IM 平台已注册且连接成功** |
| **我的数字员工卡片 / 数字员工详情** | **配置 IM** | **IM 配置页** | **实例 status=live** |
| **IM 配置页** | **点击注册·连接成功** | **当前页（展示已连接状态）** | **凭证有效，连接建立成功** |
| **平台会话页顶部** | **去 IM（拉起 IM）** | **对应 IM 应用（外部跳转）** | **至少一个 IM 平台已注册且连接成功** |
| **平台会话页顶部** | **接入 IM（未配置时）** | **IM 配置页** | **尚未配置任何 IM 平台** |
| 我的数字员工 | 查看详情 | 数字员工详情 | 用户拥有查看权限 |
| 数字员工详情 | 创建私有分支 | 私有分支定制页 | 当前实例为我的分身，且 owner 为本人 |
| Review 页 | 回退到工位 | 对应流程节点 | 用户选择回退位置 |

---

## 7. 面向现有 HTML / 原型的落地建议

### 7.1 现有 HTML / 原型中可复用的核心页面

1. 我的数字员工
2. 企业模板池
3. 部门版雇佣页
4. AI 评估页
5. 模板详情弹窗

### 7.2 需在现有 HTML / 原型中补齐的页面与能力

1. 统一后的"部门数字员工"页面，并支持角色差异视图
2. 数字员工详情页，作为全部状态与动作承接页
3. 人工评估页与 Review 页
4. 分身复制两步向导（去除飞书配置步骤，改为完成后引导）
5. 私有分支定制页
6. **部门长快捷复制确认页（弹窗或侧边栏）**
7. **站内对话页（新增，主要对话渠道）**
8. **IM 配置页（多平台 Tab，替换原飞书绑定弹窗）**
9. 按角色收敛后的顶层模块

### 7.3 需在现有 HTML / 原型中落实的调整项

1. 删除双方"工作台"模块，不再以首页概览承接主流程。
2. 删除全部反馈相关页面、入口和链路。
3. 部门长入口收敛为"企业模板池 / 部门数字员工 / 我的数字员工"。
4. 普通成员入口收敛为"部门数字员工 / 我的数字员工"。
5. `部门数字员工` 采用统一模块名，但部门长看全部状态，普通成员只看已上岗。
6. **"我的数字员工"卡片主按钮由"去飞书使用"改为"开始对话"（进入站内对话页）。**
7. **部门数字员工 / 已上岗的卡片操作菜单新增"快捷复制"入口（部门长可见）。**
8. **IM 接入不再是上岗的必须步骤，改为上岗完成后的可选引导。**
9. **原飞书绑定弹窗改造为多平台 IM 配置页（支持飞书/钉钉/企微三个 Tab）。**
10. 数字员工详情页的"飞书接入区"改为"IM 接入区"，展示三平台各自的配置状态。
11. 数字员工详情页应成为统一动作分发页，减少用户在多个列表页和流程页之间来回跳转。

---

## 8. 本次结构调整的输出价值

相较 V5，本版（V6）重点完成了七件事：

1. **新增平台会话功能。** 数字员工上岗后，用户在 Web 端点击员工卡片"开始对话"即可进入平台内嵌对话界面，零配置、无需跳转外部 IM，流式输出，历史持久化。
2. **新增拉起 IM 功能。** 完成 IM 注册后，平台在员工卡片、详情页、平台会话页顶部均提供"去 IM"快捷按钮，一键跳转到对应 IM 应用并进入与员工机器人的私聊，支持多平台选择气泡。
3. **IM 接入归属重新划定。** 部门员工发布后不配置 IM；IM 接入是**个人分身层面**的可选操作，由各成员在自己的分身上按需完成，支持飞书/钉钉/企微，配置后可随时从平台拉起。
4. **新增部门长快捷复制流程。** 部门长可从已上岗的部门员工一键复制出新员工，继承评估结果直接上岗，分钟级完成，并自动引导部门长创建分身体验。
5. **多平台 IM 支持（WebSocket / URL 回调两种模式）。** IM 配置页扩展为三平台 Tab（飞书/钉钉/企微），每平台支持 WebSocket 长连接和 URL 回调两种接入模式，凭证字段动态变化，点击"注册"后展示连接状态。
6. **页面清单更新。** 新增：平台会话页（§4.15）、IM 配置页多平台版（§4.14）、快捷复制确认页（§4.13）。
7. **跳转矩阵和流程图同步更新。** 平台会话、拉起 IM 两条路径均纳入跳转矩阵，确保页面、流程、矩阵三者一致。
