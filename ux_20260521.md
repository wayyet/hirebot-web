# 雇佣端 UX 文档

> 版本：20260521
> 基于原型设计前端代码 + 需求文档反向生成
> 参考：04-雇佣端_UX_文档-v2.md

---

## 1. 设计目标与原则

### 1.1 设计目标

雇佣端需要同时满足三类目标：

1. **完整雇佣流程**：让部门长基于模板完成部门版数字员工的完整雇佣流程（四阶段装配 + 双阶段评估），并持续查看团队内数字员工的状态变化。

2. **极简分身复制**：让部门长和普通成员可以从已有部门员工快速复制出个人分身，完成后立即在平台内通过站内对话开始使用。

3. **私有分支定制**：让分身 owner 可以按需接入 IM（飞书/钉钉/企微），或创建私有分支进行个性化定制。

**核心要求**：系统必须同时支持"完整雇佣流程"、"极简分身复制流程"和"私有分支定制流程"，但不能把三类流程混在一起。

### 1.2 开箱即用

1. 普通成员复制数字员工时，路径必须压缩到最少步骤（目标：≤ 3 步）。
2. 普通成员不进入模板池，不进入四阶段装配流程，不进入雇佣教练对话。
3. 普通成员看到的核心路径：`部门数字员工 → 创建分身 → 设置显示信息 → 上岗 → 站内对话立即可用`
4. IM 接入是个人分身层面的可选操作，不阻塞上岗流程。
5. 所有技术概念都由系统翻译为业务语言，用户不直接操作 manifest、ontology、skill 等技术对象。

### 1.3 严格门槛

1. 部门版雇佣与私有分支都必须经过双阶段评估（AI 评估 + 人工评估），不能绕过。
2. 评估失败后允许回退，但不允许以"跳过评估"的形式发布上线。
3. 分身复制（personal_clone）继承母实例的评估结果，无需重走评估，直接上岗。
4. 部门员工发布后不配置 IM；IM 接入是个人分身层面的可选操作。

### 1.4 边界清晰

1. Web 端负责雇佣、评估、配置与管理。
2. 数字员工上岗后支持两类主动发起对话的方式：
   - **站内对话（主要渠道）**：在 Web 端内嵌界面直接对话，上岗即可用，无需任何额外配置。
   - **拉起 IM（可选渠道）**：从 Web 端点击"去 {平台名}"按钮，跳转到对应 IM 应用并进入与员工机器人的私聊，需先完成 IM 注册。
3. 两种方式不互斥，底层共用同一运行时沙箱，对话上下文各自独立。

### 1.5 隐私优先

1. 部门长可以查看团队数字员工的状态、配置与元数据，但**不能查看**下属与数字员工的具体私聊内容。
2. 私有分支仅创建者本人可见、可评估、可废弃。
3. 所有页面字段展示都必须遵循：租户隔离 + 用户隔离 + 角色隔离

### 1.6 多渠道使用

1. 数字员工支持两类使用渠道：**站内对话（默认）** 和 **IM 接入（可选）**。
2. 站内对话：上岗即可用，无需额外配置，是 MVP 阶段的主要对话入口。
3. IM 接入：支持飞书、钉钉、企微，每个平台独立配置，同一员工可同时接入多个平台。
4. IM 接入后仅支持一对一私聊，不提供群聊入口。
5. 站内对话与 IM 渠道的对话历史**独立存储**，不互通。

### 1.7 文案统一

面向用户的主表达统一使用"雇佣"，推荐统一文案：

| 场景 | 推荐文案 |
|------|----------|
| 雇佣入口 | 雇佣、继续雇佣、重新雇佣 |
| 对话入口 | 开始对话 |
| IM 配置 | 配置 IM、去飞书使用 |
| 列表名称 | 部门数字员工、我的数字员工 |
| 分身创建 | 创建分身（强调"上岗即可对话，无需评估"） |

---

## 2. 角色、对象与状态

### 2.1 角色定义

| 角色 | 说明 |
|------|------|
| **部门长** | 负责查看企业模板池，发起部门版雇佣，管理部门数字员工，可查看全部状态实例并推动至上岗。同时可以创建个人分身。 |
| **普通成员** | 负责查看部门数字员工中的已上岗实例，复制出个人分身，通过站内对话直接使用，并可按需接入 IM。 |
| **分身 Owner** | 指个人分身的拥有者，只有 owner 可以为自己的 personal_clone 创建私有分支、配置 IM、开始对话。 |

### 2.2 核心对象

| 对象类型 | 说明 |
|----------|------|
| **模板 template** | 雇佣起点，只能被浏览和选择，不能在雇佣端直接生产或派生新模板。 |
| **部门员工 department** | 由部门长基于模板走完整四阶段装配 + 双阶段评估创建，面向本部门复用。**不配置 IM**。 |
| **我的分身 personal_clone** | 由用户基于部门员工快照复制得到，独立使用，继承父实例评估结果直接上岗，可配置 IM。 |
| **私人定制 private_branch** | 由 personal_clone 的 owner 基于现有分身进行个性化定制得到，走完整双阶段评估，仅 owner 可见，拥有独立 IM 配置。 |

### 2.3 状态定义

| 状态值 | 显示文本 | 说明 | 可见性 |
|--------|----------|------|--------|
| `hired` | 已雇佣 | 四阶段装配进行中 | 仅雇佣者 |
| `interning_ai` | AI 评估中 | 双沙箱模式评估中 | 仅雇佣者 |
| `interning_human` | 人工评估中 | 等待部门长/用户自评 | 仅雇佣者 |
| `live` | 已上岗 | 站内对话立即可用，可按需配置 IM | 部门员工：全部门；分身/私人定制：仅 owner |
| `error` | 异常 | 沙箱连续3次冷启动失败，暂时不可用 | 仅 owner + 平台运维 |
| `failed` | 评估失败 | 等待进入 Review 决定回退位置 | 仅雇佣者 |
| `retired` | 已退役 | 只读历史 | 仅 owner |

### 2.4 状态与模块映射

**部门长视图（部门数字员工）：**

- 已雇佣：对应 `hired`
- 待实习：包含两个子标签
  - AI 评估中：对应 `interning_ai`
  - 人工评估中：对应 `interning_human`
- 已上岗：对应 `live`
- 失败/异常：`failed` 和 `error` 通过详情页承接

**普通成员视图（部门数字员工）：**

- 只显示已上岗（`live`）的部门员工
- 提供"创建分身"入口

### 2.5 状态与按钮联动

| 状态 + 类型 + 角色 | 主按钮 | 次要动作 |
|---|---|---|
| `live`（部门员工 + 部门长） | — | 创建分身、重新雇佣 |
| `live`（部门员工 + 普通成员） | 创建分身 | — |
| `live`（我的分身 / 私人定制 + owner） | **开始对话** | 去 IM、配置 IM、创建私有分支、退役 |
| `hired` | 继续雇佣 | — |
| `interning_ai` | 查看 AI 评估进度 | — |
| `interning_human` | 进入人工评估 | — |
| `failed` | 查看评估报告、继续雇佣 | — |
| `error` | 查看错误详情 | 退役 |
| `retired` | 查看历史 | 重新复制 / 重新雇佣 |

### 2.6 权限边界

1. 部门长可以查看本部门所有部门员工的状态、评估进度和上岗情况。
2. 部门长的"我的数字员工"仅展示其本人拥有的"我的分身"和"私人定制"。
3. 普通成员在"部门数字员工"中只能看本部门 `live` 的"部门员工"。
4. 非 owner 不展示"开始对话"入口和会话历史入口。
5. 私有分支详情仅 owner 可见。

---

## 3. 信息架构

### 3.1 顶层模块

**部门长模块：**
1. 企业模板池
2. 部门数字员工
3. 我的数字员工

**普通成员模块：**
1. 部门数字员工
2. 我的数字员工

### 3.2 页面分类

**列表/模块页：**
- 企业模板池
- 部门数字员工
- 我的数字员工

**详情页：**
- 数字员工详情
- 模板详情

**流程页：**
- 部门版雇佣页（四阶段装配：左侧会话区 + 右侧待办区）
- AI 评估页
- 人工评估页
- Review 页（评估失败后回退）
- 上岗确认页（人工评估通过后发布）
- 分身复制向导（三步：确认复制 → 设置显示信息 → 上岗）
- 私有分支定制页（简化模式雇佣教练 + 双阶段评估）
- IM 配置页（多平台：飞书 / 钉钉 / 企微）

**对话页：**
- **站内对话页（主要对话渠道，上岗即可用）**

**共享组件：**
- IM 配置引导弹层（多平台）
- AI 评估进度卡片
- 分身数量上限提示

### 3.3 核心入口策略

1. 部门长从"企业模板池"进入完整四阶段装配流程。
2. 部门长从"部门数字员工"管理本部门所有状态实例。
3. 部门长和普通成员从"部门数字员工"的已上岗员工进入分身复制向导。
4. 用户从"我的数字员工"的员工卡片**直接进入站内对话**（主要使用入口）。
5. 用户从员工卡片或详情页进入 IM 配置，配置完成后可点击"去 {平台名}"拉起 IM。
6. 数字员工详情页承担所有状态承接与动作分发。

---

## 4. 数据模型

### 4.1 模板 (Template)

```javascript
{
  id: string,              // 模板唯一标识，格式：tpl_xxx
  name: string,            // 模板名称，如"HR 助手"
  initial: string,         // 首字母，如"H"
  tint: string,            // 主题色：green|orange|blue|purple|pink|gray
  source: string,          // 来源：全局通用 | 企业专属
  summary: string,         // 简介描述
  sectors: string[],       // 适用部门，如["HR", "招聘", "员工关系"]
  used: number,            // 已被多少部门使用
  cloned: number,          // 被复制次数
  capabilities: string[],  // 能力清单（可做事项）
  cants: string[],         // 边界/禁做事项
  tags: string[]           // 标签，如["AI增强", "信息处理"]
}
```

### 4.2 数字员工 (Employee)

```javascript
{
  id: string,              // 实例唯一标识
  type: string,            // 实例类型：department | personal_clone | private_branch
  name: string,            // 显示名称
  initial: string,         // 头像首字母
  tint: string,            // 主题色
  template: string,        // 关联模板ID
  status: string,          // 状态：hired|interning_ai|interning_human|live|error|failed|retired
  desc: string,           // 描述文字
  owner: string,           // 所有者名称
  dept: string,           // 所属部门
  updated: string,        // 更新时间，格式如"2 小时前"、"今天 11:24"
  tags: string[],          // 标签
  // 状态相关字段
  runs: number,           // 累计对话轮次
  cloned: number,         // 被复制数
  activeAt: string,       // 最近活跃时间
  evalProgress: number,   // 评估进度（百分比）
  hireProgress: number,   // 雇佣流程进度（步数，如 4/6）
  failedReason: string,   // 失败原因，如"差距挖掘"、"能力生成"
  // 来源关系
  parent: string,         // 父实例ID（分身复制源）
  parentName: string,     // 父实例名称
  // IM绑定
  seedBindings: {
    [channelId: string]: {
      channelId: string,    // 渠道ID：lark|dingding|wecom
      methodId: string,     // 连接方式：websocket|callback
      status: string,       // 连接状态：connected|error|unconfigured
      connectedAt: string, // 连接时间
      form: object         // 配置表单数据
    }
  }
}
```

### 4.3 IM 渠道元数据

```javascript
IM_CHANNEL_META = {
  lark: { name: "飞书", short: "飞", accent: "blue" },
  dingding: { name: "钉", short: "钉", accent: "orange" },
  wecom: { name: "企微", short: "企", accent: "green" }
}
```

### 4.4 状态标签映射

```javascript
STATUS_LABEL = {
  hired: { text: "已雇佣", cls: "hired", desc: "等待进入评估" },
  interning_ai: { text: "AI 评估中", cls: "ai", desc: "AI 自动化评估" },
  interning_human: { text: "人工评估中", cls: "human", desc: "用户在场景轮次中评估" },
  live: { text: "已上岗", cls: "live", desc: "可在站内对话或 IM 中使用" },
  failed: { text: "评估失败", cls: "failed", desc: "等待 Review 决策" },
  retired: { text: "已退役", cls: "retired", desc: "只读" }
}
```

### 4.5 类型标签映射

```javascript
TYPE_LABEL = {
  template: "模板",
  department: "部门员工",
  personal_clone: "我的分身",
  private_branch: "私人定制"
}
```

---

## 5. 页面清单与详细说明

### 5.1 企业模板池（TemplatesPage）

**入口**：顶部导航 → 企业模板池（部门长专属）

**页面目标**：供部门长浏览企业模板和全局模板，并选择一个模板开始雇佣部门版。

**核心模块**：

1. **搜索区**
   - 关键词搜索（名称、场景、能力关键词）
   - 不提供其他筛选器

2. **模板列表区**
   - 卡片化展示
   - 筛选Tab：全部 / 全局通用 / 企业专属

**卡片字段**：
- 模板名称与头像
- 模板来源标签（全局通用 / 企业专属）
- 适用部门
- 场景简介
- 能力标签
- 统计数据（部门已用数、复制数）
- 查看详情入口

**关键规则**：
- 不提供"派生新模板"、"编辑模板源码"等入口
- 部门长只能基于模板创建部门版，不能在雇佣端生产模板
- 从模板卡片进入详情，详情页提供"雇佣"按钮作为唯一起点

---

### 5.2 模板详情（TemplateDetailPage）

**入口**：模板卡片点击

**页面目标**：帮助部门长判断模板是否适合当前部门，作为唯一的部门版雇佣起点。

**核心模块**：

1. **模板概览**
   - 头像 + 名称 + 来源标签
   - 适用部门
   - 简介描述

2. **能力清单**
   - 可做事项（✓ 绿色标记）
   - 不可做事项（× 红色标记）

3. **模板概览数据**
   - 来源
   - 适配部门
   - 部门采用数
   - 被复制数

4. **主动作区**
   - 雇佣按钮（部门长可见）

---

### 5.3 部门数字员工（DeptPage）

**入口**：顶部导航 → 部门数字员工

**页面目标**：以统一模块名承接部门级数字员工资产，但根据角色提供不同视图。

**部门长视图**：

1. **一级Tab**：已雇佣、待实习、已上岗
2. **二级Tab**（待实习下）：AI 评估中、人工评估中
3. **列表区**：卡片化展示

**普通成员视图**：

1. 仅显示"已上岗"
2. 本质上是本部门可复制的 `live` 部门员工列表

**卡片字段**：
- 对外名称与头像
- 当前状态或阶段标签
- 来源模板名称
- 最近更新时间
- 评估进度条（评估中状态）
- 失败原因提示（failed状态）

**动作规则**：

| 角色 | 状态 | 主动作 | 次要动作 |
|---|---|---|---|
| 部门长 | `hired` | 继续雇佣 | — |
| 部门长 | `interning_ai` | 查看 AI 评估 | — |
| 部门长 | `interning_human` | 进入人工评估 | — |
| 部门长 | `live` | 查看详情 | 创建分身、重新雇佣 |
| 部门长 | `failed` | 查看报告 | 继续雇佣 |
| 普通成员 | `live` | 创建分身 | 查看详情 |

**关键规则**：
- `failed` 实例不新增一级标签，通过卡片按钮进入详情/Review页承接
- 分身数量达上限（10个）时，"创建分身"按钮置灰并提示"已达分身上限（N/10）"

---

### 5.4 我的数字员工（MyPage）

**入口**：顶部导航 → 我的数字员工

**页面目标**：按个人视角管理当前用户本人拥有的分身和私人定制，提供站内对话快速入口。

**页面范围**：
- 仅显示当前用户本人拥有的"我的分身"和"私人定制"
- 不在此页展示整个团队的"部门员工"列表

**核心模块**：

1. **统计指标条**
   - 实例总数
   - 已上岗
   - 评估中
   - 私人定制
   - 待回退

2. **筛选Tab**：全部 / 已上岗 / 评估中 / 私人定制 / 待回退

3. **卡片列表**
   - IM 接入状态：飞书/钉钉/企微（已配置绿色、未配置灰色、异常红色）

**动作规则**：

| 状态 | 主按钮 | 次要动作 |
|---|---|---|
| `live` | **开始对话**（进入站内对话页） | 去 IM、配置 IM、创建私有分支、退役 |
| `interning_ai` | 查看 AI 评估进度 | — |
| `interning_human` | 进入人工评估 | — |
| `failed` | 查看报告、继续雇佣 | — |
| `error` | 查看错误详情 | 退役 |
| `retired` | 查看历史 | 重新复制 |

**关键规则**：
- `live` 状态卡片的主按钮统一为**"开始对话"**，点击直接进入站内对话页
- **"去 IM"按钮**：至少有一个 IM 平台已连接时显示；多平台已配置时弹出平台选择气泡
- 点击"去 IM"但尚未配置任何平台时：显示提示"该员工尚未接入 IM，是否立即配置？"

---

### 5.5 数字员工详情（EmployeeDetailPage）

**入口**：员工卡片点击

**页面目标**：承接单个实例的状态、来源、身份、运行情况与后续动作。

**核心模块**：

1. **基本信息区**
   - 头像 + 名称 + 状态标签 + 类型标签
   - 所属部门、Owner、更新时间
   - 描述文字

2. **来源关系区**
   - 模板 → 部门员工 → 我的分身（→ 私人定制）层级展示

3. **能力简介区**
   - 业务可读能力标签（来自模板）
   - 不展示底层技术文件

4. **运行状态区**（仅 `live` 状态）
   - 累计对话轮次
   - 被复制数
   - 最近活跃时间
   - 最近错误数
   - 实例版本

5. **IM 接入区**（仅我的分身/私人定制显示）
   - 飞书/钉钉/企微三平台各自展示一行
   - 已连接：绿色图标 + "已连接" + "去 {平台名}" 按钮
   - 未配置：灰色图标 + "未配置" + "配置 IM" 按钮
   - 配置异常：红色图标 + "配置异常" + "重新配置" 按钮

6. **操作区**
   - 根据类型与状态显示主动作

**主按钮建议**：

| 实例类型 + 状态 + 角色 | 主按钮 | 次要按钮 |
|---|---|---|
| 部门员工 + `hired` + 部门长 | 继续雇佣 | — |
| 部门员工 + `interning_ai` + 部门长 | 查看 AI 评估 | — |
| 部门员工 + `interning_human` + 部门长 | 进入人工评估 | — |
| 部门员工 + `live` + 部门长 | 创建分身 | 重新雇佣 |
| 部门员工 + `live` + 普通成员 | 创建分身 | — |
| 我的分身 + `live` + owner | **开始对话** | 创建私有分支、退役 |
| 私人定制 + `live` + owner | **开始对话** | 退役 |
| 任意 + `failed` | 查看评估报告 | 继续雇佣 |
| 任意 + `retired` | 查看历史 | 重新复制 / 重新雇佣 |

**关键规则**：
- 非 owner 不显示"开始对话"和站内对话历史入口
- 部门长查看团队内实例时，只展示元数据、状态和配置，不展示私聊明细
- 私有分支详情仅 owner 可见
- 部门员工不显示 IM 接入区

---

### 5.6 部门版雇佣页 - 四阶段装配（HireFlowPage）

**入口**：
- 模板详情 → 雇佣按钮
- 员工详情 → 继续雇佣
- Review 页 → 确认回退

**页面目标**：通过雇佣教练与用户并肩装配，完成四阶段装配流程，生成完整实例包，进入 AI 评估。

#### 5.6.0 雇佣流程体系说明

代码中存在**三套流程定义**，对应不同层级的抽象：

| 流程 | 步骤数 | 用途 |
|------|--------|------|
| `HIRE_STEPS` | 6步 | 旧版 discovery 流程（已废弃） |
| `HIRE_PHASES` | 6阶段 | 雇佣教练对话的完整阶段划分 |
| `PROCESS_STEPS` | 4步 | 用户可见的四阶段装配（UI展示层） |

**阶段映射关系**：

```
HIRE_PHASES (6阶段)          PROCESS_STEPS (4步)
─────────────────────────────────────────────────
阶段1: 了解数字员工         →  (引导页，不计入4步)
阶段2: 上传业务资料         →  ① 业务资料
阶段3: 文件解析与循环补全   ↗
阶段4: 配置技能模块         →  ② 技能模块
阶段5: 对接外部系统         →  ③ 外部系统
阶段6: 生成实例包           →  ④ 生成实例
```

**HIRE_PHASES 详细定义**：

```javascript
HIRE_PHASES = [
  { id: 1, title: "了解数字员工", description: "完成自我介绍，理解雇佣流程与整体目标。" },
  { id: 2, title: "上传业务资料", description: "上传知识库、产品手册、FAQ和服务话术。" },
  { id: 3, title: "文件解析与循环补全", description: "持续归档已识别材料，追问缺失项直到补齐。" },
  { id: 4, title: "配置技能模块", description: "采纳推荐技能，或上传/生成新的执行能力。" },
  { id: 5, title: "对接外部系统", description: "配置CRM、OMS、工单系统和IM接入平台。" },
  { id: 6, title: "生成实例包", description: "打包资料、技能和系统配置，生成可部署成果。" }
]
```

#### 5.6.1 整体布局

```
┌────────────────────────────────────────────────────────────────┐
│  顶部进度条（固定显示）：                                          │
│  ① 业务资料   ② 技能模块   ③ 外部系统   ④ 生成实例              │
│  ─────────── 当前阶段高亮，已完成阶段标绿色对勾 ──────────────────  │
├───────────────────────┬────────────────────────────────────────┤
│                       │                                        │
│   左侧：会话区          │     右侧：待办区                        │
│   （固定）             │     （分阶段渲染，始终可见）               │
│                       │                                        │
│ · 雇佣教练 对话         │  ① 业务资料阶段：附件展示区               │
│ · 用户文字输入          │  ② 技能模块阶段：Skill 列表               │
│ · 文件上传（附件）      │  ③ 外部系统阶段：系统配置表单              │
│                       │  ④ 生成实例阶段：确认生成按钮            │
│                       │                                        │
│                       │  ── 当前阶段高亮，其余阶段折叠 ──          │
└───────────────────────┴────────────────────────────────────────┘
│  底部操作区：保存退出   ·   进入 AI 评估（仅①②③ 全完成后激活）    │
└────────────────────────────────────────────────────────────────┘
```

#### 5.6.2 顶部进度条

| 阶段 | 说明 | 对应 HIRE_PHASES |
|---|---|---|
| ① 业务资料 | 上传业务文档与场景描述，触发知识本体抽取 | 阶段2~3 |
| ② 技能模块 | 采纳推荐技能，或上传/生成新的执行能力 | 阶段4 |
| ③ 外部系统 | 填写需接入的外部系统，触发配置草案生成（无外部系统可跳过） | 阶段5 |
| ④ 生成实例 | 前三阶段全部完成后，一键生成完整实例包，进入 AI 评估 | 阶段6 |

**进度条状态**：
- 当前阶段：高亮蓝色，带进行中动效
- 已完成阶段：绿色 + 对勾图标
- 待解锁阶段：灰色，不可点击

#### 5.6.3 左侧会话区

- 雇佣教练以**数字员工身份**开场，介绍自己的使命，引导用户完成当前阶段待办
- 对话气泡区：用户消息（右对齐）+ 雇佣教练回复（左对齐），支持流式输出
- 底部输入区：文本框（Enter 发送，Shift+Enter 换行）+ 附件上传按钮
- 附件上传按钮在任何阶段均可见，上传后在右侧附件区展示

#### 5.6.4 右侧待办区（分阶段渲染）

##### 阶段①：业务资料

**外观**：右侧面板展示已上传材料的附件列表

**附件条目字段**：
- 文件名 + 文件图标
- 文件大小
- 上传时间
- 状态标签：上传中 / 已上传 / 处理中 / 已就绪

**待办状态标签**：

| 状态 | 样式 | 含义 |
|---|---|---|
| 信息收集中 | 橙色标签 | 信息不足，雇佣教练引导补充 |
| 处理中 | 蓝色标签 + 旋转图标 | 信息充分，ontology-extraction 运行中 |
| 已完成 | 绿色标签 + 对勾 | 本体抽取完成，自动生成测试用例 |

**阶段推进条件**：至少一条资料待办状态为"已完成"，自动解锁②阶段

##### 阶段②：技能模块

**外观**：右侧面板展示 Skill 列表，分为两组

- **模板已有 Skill**（灰色区块）：来自模板的原有技能，只读，可展开查看详情
- **新增/生成的 Skill**（白色区块）：本次装配过程中生成或上传的技能，可展开、可确认、可删除

**Skill 条目字段**：
- Skill 名称（英文 slug）+ 中文标签
- 用途描述（description）
- 来源标签：对话生成 / 文件上传 / 模板自带
- 状态：待确认 / 已确认 / 处理中

**技能输入方式**：
1. **会话生成**：在左侧会话框描述技能需求，雇佣教练生成草稿
2. **上传 Skill 文件**：右侧面板内提供"上传 Skill 文件"入口

**阶段推进条件**：至少一条新增 Skill 状态为"已确认"，自动解锁③阶段

##### 阶段③：配置外部系统

**外观**：右侧面板展示外部系统列表，每个系统对应一行配置条目

**系统条目字段**：
- 系统名称（如：企业微信、CRM、ERP）
- 配置状态：未配置 / 配置中 / 已完成 / 已跳过
- 【配置】按钮：点击展开填写表单

**接入类型**：支持四种类型，各自展示不同的配置表单

**① CLI（命令行工具调用）**

用于调用本地或容器内的命令行工具。

```
接入类型：CLI
─────────────────────────────────────────────
工具名称        [填写工具的可执行名称，如 jq / ffmpeg]
描述            [这个工具做什么，何时触发调用]
调用方式        ○ 直接执行  ○ 沙箱执行
允许的参数模板  [可选；预设参数格式，如 --input {file} --output {out}]
─────────────────────────────────────────────
```

关键规则：
- 每个 CLI 工具对应一条配置条目，支持添加多条
- 调用方式选"沙箱执行"时，平台提示该工具将在隔离环境运行，不可访问宿主文件系统

---

**② MCP（Model Context Protocol 工具集）**

用于接入 MCP server 暴露的工具集合。

```
接入类型：MCP
─────────────────────────────────────────────
MCP Server 地址  [填写 server endpoint URL]
认证方式         ○ 无认证  ○ API Key  ○ OAuth
  └─ API Key：[输入框]
工具勾选         ☑ 工具A    ☑ 工具B    ☑ 工具C
                 （仅展示此账号有权调用的工具；
                   无权调用的工具不显示）
─────────────────────────────────────────────
```

关键规则：
- 工具列表从 MCP server 动态拉取，仅展示当前账号实际有权调用的工具
- 无权调用的工具**不显示**，不展示为置灰或禁用状态
- 默认全选；用户可取消勾选不需要的工具

---

**③ Database（数据库查询）**

用于让数字员工读写企业数据库。

```
接入类型：Database
─────────────────────────────────────────────
数据库类型       [MySQL / PostgreSQL / 其他]
连接地址         [host:port]
数据库名         [database name]
账号             [username]
密码             [password，输入后隐藏显示]

可访问的表       ┌──────────────────────────────┐
（只读展示）      │ ✓ orders         read         │
                 │ ✓ customers      read/write    │
                 │ ✓ products       read         │
                 └──────────────────────────────┘
                 表权限由数据库账号的实际授权决定，
                 平台仅做展示，不支持在此修改权限。
─────────────────────────────────────────────
```

关键规则：
- 填写连接信息并验证通过后，平台自动拉取该账号可访问的表及其读写权限
- 权限列表为只读展示，不可在平台侧勾选或修改
- 连接验证失败时，提示"连接失败，请检查地址和账号信息"

---

**④ RAG（知识库检索）**

用于为数字员工挂载运行时可检索的文件知识库。

```
接入类型：RAG
─────────────────────────────────────────────
知识库名称       [填写，用于区分多个知识库]
文件上传         [拖拽或点击上传，支持 PDF / DOCX / TXT / MD]
                 ┌─────────────────────────────────────────┐
                 │  这里上传的文件用于运行时检索，不是员工背景知识  │
                 └─────────────────────────────────────────┘
已上传文件        合同模板v3.pdf          [删除]
                 产品手册2024.docx       [删除]
检索策略         ○ 语义检索  ○ 关键词检索  ○ 混合检索
─────────────────────────────────────────────
```

关键规则：
- 提示文字"这里上传的文件用于运行时检索，不是员工背景知识"始终展示在上传区域上方
- 本区域上传的文件进入运行时检索索引，与会话框上传的知识文档（影响本体抽取）严格区分
- 支持上传多个文件，每个文件独立显示上传状态（解析中 / 已就绪 / 解析失败）
- 一个外部对接条目可挂载多个 RAG 知识库

---

**无外部系统的处理**：
- 提供"此员工无需对接外部系统"勾选项
- 勾选后③阶段标记为"已跳过"（绿色跳过标签），不阻塞④阶段

**阶段推进条件**：至少一条外部系统已完成，或标记为"跳过"，自动解锁④阶段

##### 阶段④：生成实例包

**外观**：右侧面板底部展示"生成实例包"大按钮

**触发条件**：
- ①②③三阶段的所有必要待办均已完成（100%）
- 不满足则按钮置灰

**生成中状态**：
- 按钮变为"生成中…"+ 进度动效
- 提示文字："正在汇总三阶段产物，测试用例自动打包（预计 10-30 秒）"

**生成完成**：
- 提示"实例包已生成，即将进入 AI 评估"
- 跳转到 AI 评估页

#### 5.6.5 保存与恢复机制

- 任意时刻点击"保存退出"，当前会话历史、待办状态、已上传文件全部持久化保存
- 再次从部门员工列表点击"继续雇佣"，进入同一个会话窗口和沙箱，状态自动恢复
- 一个雇佣任务对应唯一一个会话窗口，不能新建第二个窗口

---

### 5.7 AI 评估页（EvalAIPage）

**入口**：四阶段装配完成后自动跳转；或从部门员工列表卡片"查看 AI 评估"进入

**页面目标**：展示 AI 评估进度，呈现评估结果，供部门长决定是否进入人工评估或 Review。

#### 5.7.1 评估进行中状态

```
┌──────────────────────────────────────────────────────────┐
│  顶部：数字员工基本信息（名称 + 头像 + 状态：AI 评估中）       │
├──────────────────────────────────────────────────────────┤
│  评估进度卡片：                                            │
│  · 进度条：3 / 10 个用例已完成                             │
│  · 当前正在执行：用例名称                                   │
│  · 预计剩余时间                                           │
│                                                          │
│  用例列表（滚动区）：                                       │
│  ✓ 用例 1：{名称} ── PASS                                │
│  ✓ 用例 2：{名称} ── PASS                                │
│  ⟳ 用例 3：{名称} ── 执行中…                              │
│  ○ 用例 4：{名称} ── 等待                                 │
└──────────────────────────────────────────────────────────┘
│  底部：返回列表（可安全离开，评估后台继续执行）                  │
└──────────────────────────────────────────────────────────┘
```

**考题确认卡片**（评估初始化阶段）：
- 测试用例列表（输入描述 + 判卷标准）
- 提示："以上用例来自雇佣过程中的场景描述和自动生成，确认后开始评估"
- 【确认开始评估】按钮

#### 5.7.2 评估完成状态（PASS）

```
┌──────────────────────────────────────────────────────────┐
│  顶部：AI 评估结果 — PASS                                  │
│  综合得分：82 分                                           │
├──────────────────────────────────────────────────────────┤
│  各维度得分（动态维度，由评估本体决定）：                      │
│  · 功能完整性    90分  ████████████████░░░░  权重 25%     │
│  · 交互质量      82分  ████████████████░░░░  权重 30%     │
│  · 流程合规性    88分  ████████████████░░░░  权重 15%     │
│  · 问题解决率    80分  ████████████████░░░░  权重 20%     │
│  · 工具调用准确  95分  ████████████████████  权重 10%     │
│                                                          │
│  一票否决项：全部通过 ✓                                    │
│  [查看完整评估报告]                                        │
└──────────────────────────────────────────────────────────┘
│  底部：[进入人工评估]（主按钮）   [返回列表]（次要）           │
└──────────────────────────────────────────────────────────┘
```

#### 5.7.3 评估完成状态（FAIL）

```
┌──────────────────────────────────────────────────────────┐
│  顶部：AI 评估结果 — FAIL                                  │
│  综合得分：52 分                                           │
├──────────────────────────────────────────────────────────┤
│  失败原因概要：                                            │
│  · 工具调用准确性 0分（遗漏必须调用工具，一票否决）            │
│  · 流程合规性 45分（低于单维度 60 分阈值）                   │
│                                                          │
│  失败用例列表：                                            │
│  ✗ 用例 3：{名称} ── FAIL ── 查看详情                     │
│  ✗ 用例 7：{名称} ── FAIL ── 查看详情                     │
│                                                          │
│  [查看完整评估报告]                                        │
└──────────────────────────────────────────────────────────┘
│  底部：[进入 Review 页]（主按钮）   [重新评估]（上限 2 次）   │
└──────────────────────────────────────────────────────────┘
```

---

### 5.8 人工评估页（EvalHumanPage）

**入口**：AI 评估通过后点击"进入人工评估"；或从待实习列表直接进入

**页面目标**：部门长（部门员工）或用户自己（私有分支）与数字员工真实对话，判断是否达到上岗标准。

#### 5.8.1 页面布局

```
┌──────────────────────────────────────────────────────────┐
│  顶部信息区：                                              │
│  · 员工名称 + 头像 + 状态（人工评估中）                      │
│  · AI 评估摘要：综合得分 82 分 / PASS                      │
├──────────────────────────────────────────────────────────┤
│  会话区（人工评估对话界面）：                                │
│  · 与员工真实对话，测试业务场景                              │
│  · 支持文字输入、文件上传                                   │
│  · 员工回复流式输出                                        │
│  · 对话历史持久化，可上拉加载更多                           │
│                                                          │
│  底部输入区：文本框 + 附件上传 + 发送                       │
├──────────────────────────────────────────────────────────┤
│  操作区：                                                  │
│  [发布上岗]（主按钮，仅部门长可见）  [不通过，进入 Review]     │
└──────────────────────────────────────────────────────────┘
```

**关键规则**：
1. 加载的是 AI 评估完成后的实例包（不包含测试用例），员工按真实配置运行
2. 人工评估无量化分数要求，部门长的主观判断即为最终结论
3. 部门长可以推翻 AI 评估结论（Override），但需填写原因
4. 普通成员无"发布上岗"按钮（发布权限仅部门长）
5. 私有分支的人工评估由 owner 自行执行，评估通过后自己发布

---

### 5.9 Review 页（ReviewPage）

**入口**：AI 评估 FAIL 后点击"进入 Review 页"；或人工评估"不通过"后

**页面目标**：展示评估报告全文，引导用户选择回退到哪个阶段重新修改。

#### 5.9.1 页面布局

```
┌──────────────────────────────────────────────────────────┐
│  顶部：评估报告全文（可滚动）                                │
│  · 综合得分与结论                                          │
│  · 各维度得分与证据                                        │
│  · 一票否决项命中情况                                      │
│  · 改进建议（含优先级标注）                                  │
├──────────────────────────────────────────────────────────┤
│  回退环节选择区：                                          │
│  [① 上传资料]  [② 补齐技能]  [③ 配置外部系统]  [④ 重新生成] │
│  · 选中阶段高亮，显示"将保留哪些内容"的说明                  │
└──────────────────────────────────────────────────────────┘
│  底部：[确认回退，重新雇佣]   [暂不处理，返回列表]            │
└──────────────────────────────────────────────────────────┘
```

**回退逻辑说明**：

| 选择回退到 | 保留内容 | 重新执行 |
|---|---|---|
| ① 上传资料 | 仅保留已上传源文件 | 从①开始重跑全流程 |
| ② 补齐技能 | 保留①产物 + 已有技能配置 | 从②开始重跑 |
| ③ 外部系统 | 保留①②产物 | 从③开始重新配置 |
| ④ 重新生成 | 保留①②③全部产物 | 仅重新打包生成 |

**关键规则**：
1. 会话历史在回退后保留，用户可翻看上一轮的交流内容
2. 连续失败 3 次后，系统在报告顶部显示建议提示

---

### 5.10 上岗确认页

**入口**：人工评估页点击"发布上岗"

**页面内容**：
- 确认弹窗或单独页面
- 展示即将上岗的员工基本信息（名称、头像）
- 说明上岗后的效果：
  - 部门员工：出现在部门数字员工列表，部门成员可见并创建分身
  - 私人定制：仅 owner 可见，作为私有分支上岗
- 【确认发布】主按钮

---

### 5.11 分身复制向导（CloneWizard）

**入口**：部门员工详情页/列表卡片 → "创建分身"

**页面目标**：以最少步骤帮助用户从部门员工快照复制出一个上岗的个人分身。

**目标**：点击"创建分身"到完成上岗，用户操作步骤不超过 3 步，平台侧 P95 ≤ 30 秒。

#### 步骤一：确认复制

- 来源员工信息：名称、头像、来源模板、能力简介
- 说明文字："创建分身后，你将获得这个员工的完整副本，独立运行，不受原员工升级影响"
- 【确认创建分身】主按钮

#### 步骤二：设置显示信息

- `display_name`（必填）：分身的平台显示名，同一用户下不能重名
- `display_avatar`（可选）：默认沿用部门员工头像，可上传自定义头像
- `display_description`（可选）：个人简介

#### 步骤三：完成上岗

- 动效：员工头像 + "上岗成功！"
- 说明文字：
  - "站内对话已可使用，点击下方按钮立即开始"
  - "如需在飞书/钉钉/企微中使用，可在员工卡片上配置 IM"
- 【立即开始对话】主按钮（进入站内对话页）
- 【去配置 IM】次要按钮（可选）
- 【返回我的数字员工】次要链接

**关键规则**：
1. 分身复制成功后，员工状态直接为 `live`，不需要评估
2. IM 配置为可选操作，不阻塞上岗
3. 分身数量达到上限时（10个），步骤一展示禁用状态和提示

---

### 5.12 私有分支定制页（BranchCustomizePage）

**入口**："我的分身"详情页 → "创建私有分支"

**页面目标**：在已有个人分身基础上，通过雇佣教练简化模式进行个性化定制，经双阶段评估后上岗。

**与部门版雇佣的差异**：

| 差异项 | 部门版雇佣 | 私有分支定制 |
|---|---|---|
| 右侧初始内容 | 空（等待装配） | **预载父分身的①②③三阶段产物** |
| 会话开场 | 介绍自己，引导上传资料 | 告知已预载的产物，询问想修改什么 |
| 阶段解锁 | 按序逐一解锁 | 全部阶段直接解锁（可直接定位修改） |
| 评估者 | 部门长 | **分身 owner 自己** |

**预载产物说明**：

| 阶段 | 预载内容 | 说明 |
|------|----------|------|
| ① 业务资料 | 本体切片摘要 | 显示父分身的知识本体摘要（**不是原始文件**），提供"查看摘要"按钮展开详情 |
| ② 技能模块 | Skill 列表 | 显示父分身已配置的 Skill 列表（可修改、可新增、可删除） |
| ③ 外部系统 | 外部系统配置 | 显示父分身已配置的外部系统列表（可修改、可新增、可跳过） |

**定制流程**：
1. 雇佣教练以简化模式开场："我已预加载了你的分身配置，你想修改哪部分？"
2. 用户描述修改意图
3. 雇佣教练定位到对应阶段，引导针对性修改
4. 未触及的阶段保持原产物不变
5. 修改完成后，手动点击"生成新实例包"，进入 AI 评估

---

### 5.13 站内对话页（ChatPage）

**入口**："我的数字员工"卡片 → "开始对话"；分身详情页 → "开始对话"

**页面目标**：提供在平台 Web 界面内与数字员工进行对话的完整体验，上岗即可用。

#### 5.13.1 页面布局

```
┌──────────────────────────────────────────────────────────┐
│  顶部导航：                                                │
│  ← 返回  [员工头像] {员工名称}  [状态指示灯]  [更多操作▼]  │
│  更多操作：清空对话、去 IM（已配置时显示）                    │
├──────────────────────────────────────────────────────────┤
│  消息区（滚动）：                                          │
│                                    [用户消息] ────────── │
│  [员工头像]                                              │
│  员工回复（流式输出）                                      │
│  [时间戳]                                                │
│                                                          │
│  (历史消息·上拉加载更多)                                   │
│                                                          │
│  · 默认展示最近 50 条消息                                  │
│  · 沙箱启动中：显示"员工正在准备中，请稍候…"转圈动效         │
├──────────────────────────────────────────────────────────┤
│  底部输入区：                                              │
│  [附件]  [         输入消息…          ]  [发送]           │
│            Enter 发送 · Shift+Enter 换行                 │
└──────────────────────────────────────────────────────────┘
```

**关键规则**：
- 仅 owner 可与自己的分身对话
- 默认展示最近 50 条消息
- 沙箱启动中显示准备中动效

---

### 5.14 IM 配置页（IMConfigPage）

**入口**：员工详情页/卡片 → "配置 IM"

**页面目标**：配置数字员工的 IM 接入平台，支持飞书/钉钉/企微三平台。

**支持平台**：

| 平台 | 连接方式 | 配置项 |
|---|---|---|
| 飞书 | WebSocket 长连接 / URL 回调 | appId, appSecret |
| 钉钉 | URL 回调 | appId, appSecret, secretEncryptKey, token |
| 企微 | WebSocket 长连接 | appId, appSecret |

**页面内容**：
- 各平台独立配置卡片
- 连接状态显示（已连接/未配置/配置异常）
- 连接时间记录
- 重新配置入口

**关键规则**：
- 同一员工可同时接入多个平台
- IM 接入是可选操作，不阻塞上岗
- 已连接时显示"去 {平台名}"按钮可直接拉起 IM

---

## 6. 组件库

### 6.1 基础组件

| 组件 | 说明 | 关键 Props |
|---|---|---|
| `Avatar` | 头像组件 | `initial`(首字母), `tint`(颜色), `size`(尺寸) |
| `StatusPill` | 状态标签 | `status`(状态值) |
| `TypePill` | 类型标签 | `type`(类型值) |
| `Modal` | 模态框 | `open`, `onClose`, `title`, `sub`, `children`, `footer`, `lg` |
| `Toast` / `ToastHost` | 轻提示 | `toasts` 数组 |
| `Crumb` | 面包屑导航 | `label`, `onClick` |

### 6.2 业务组件

| 组件 | 说明 |
|---|---|
| `EmployeeCard` | 数字员工卡片，含头像、名称、状态、标签、进度条等 |
| `IMStatusStrip` | IM状态条，显示各平台连接状态 |
| `Lineage` | 来源关系链，模板→部门员工→分身→私人定制 |
| `StatStrip` | 统计指标条 |
| `StepsBar` | 步骤进度条 |
| `IMPickerModal` | IM跳转选择器弹层 |

### 6.3 页面组件

| 组件 | 说明 |
|---|---|
| `TopNav` | 顶部导航栏，含角色切换 |
| `TemplatesPage` | 模板池页面 |
| `DeptPage` | 部门数字员工页面 |
| `MyPage` | 我的数字员工页面 |
| `TemplateDetailPage` | 模板详情页面 |
| `EmployeeDetailPage` | 员工详情页面 |
| `HireFlowPage` | 雇佣四阶段装配页面 |
| `ChatPage` | 站内对话页面 |

---

## 7. 设计规范

### 7.1 色彩系统

**主题色**：
```css
--c-blue: #4a6cf7;        /* 主色 */
--c-success: #10b981;     /* 成功/live */
--c-warn: #c47a26;        /* 警告 */
--c-pink: #be3a8a;        /* 粉色/failed */
--c-purple: #6a5acd;      /* 紫色/人工评估 */
```

**标签背景/文字**：
| 标签色 | 背景色 | 文字色 |
|--------|--------|--------|
| blue | #e8edff | #4a6cf7 |
| orange | #fef1e3 | #c47a26 |
| green | #e6f5ec | #15803d |
| gray | #efefef | #525252 |
| pink | #fde2e8 | #be185d |
| purple | #ece7fb | #6a5acd |

**状态色彩**：
| 状态 | 色彩 |
|------|------|
| live | #10b981 |
| interning_ai | #4a6cf7 |
| interning_human | #6a5acd |
| failed | #be3a8a |
| retired | #737373 |

### 7.2 圆角规范
```css
--r-sm: 8px;
--r-md: 12px;
--r-lg: 16px;
--r-xl: 20px;
--r-pill: 999px;
```

### 7.3 阴影规范
```css
--shadow-card: 0 4px 16px rgba(0, 0, 0, 0.04);
--shadow-lift: 0 8px 24px rgba(0, 0, 0, 0.06);
--shadow-cta: 0 4px 12px rgba(0, 0, 0, 0.12);
--focus-ring: 0 0 0 3px rgba(74, 108, 247, 0.2);
```

### 7.4 字体规范
```css
font-family: -apple-system, "PingFang SC", "HarmonyOS Sans SC", "Microsoft YaHei", system-ui, sans-serif;
font-size: 14px;
line-height: 1.5;
font-variant-numeric: tabular-nums;  /* 数字等宽 */
```

### 7.5 页面背景
```css
--page-gradient: 淡蓝色渐变背景，从浅蓝(#a9d4ea)到白色过渡
```

---

## 8. 交互说明

### 8.1 角色切换
- 页面顶部提供部门长/普通成员切换按钮
- 切换后界面展示不同操作入口和视图
- 当前角色以 Avatar 形式展示在右上角

### 8.2 卡片交互
- 点击卡片进入详情页
- 待评估状态卡片点击直接进入对应评估页面
- 已上岗卡片点击进入站内对话

### 8.3 IM 跳转
- 已连接状态显示"去IM"按钮
- 多平台时弹出平台选择器
- 点击后模拟拉起对应IM平台私聊

### 8.4 雇佣流程
- 6步对话式流程（原型中 HIRE_STEPS）
- 左侧对话，右侧待办
- 支持回看中（其他步骤高亮）

---

## 9. 状态流转

### 9.1 雇佣流程状态
```
模板 → 已雇佣 → AI评估中 → 人工评估中 → 已上岗
                    ↓
              评估失败 → Review → 继续雇佣 / 回退
```

### 9.2 分身创建流程
```
部门员工 → 我的分身（继承评估结果，直接上岗）
                ↓
          私人定制（需重新评估）
                ↓
          评估失败 → Review
```

### 9.3 退役流程
```
已上岗 → 已退役（保留历史记录）
```

---

## 10. 页面路径映射

| 功能 | 路径 |
|------|------|
| 模板池 | #templates |
| 部门员工 | #dept |
| 我的员工 | #my |
| 模板详情 | #template/{id} |
| 员工详情 | #employee/{id} |
| 雇佣流程 | #hire/new 或 #hire/{id} |
| AI评估 | #eval-ai/{id} |
| 人工评估 | #eval-human/{id} |
| 评估报告 | #review/{id} |
| 站内对话 | #chat/{id} |
| IM配置 | #im/{id} |
| 快捷复制 | #quick-clone/{id} |
| 创建分身 | #clone/{id} |
| 创建分支 | #branch/{id} |
